import { useEffect,useState } from "react";import { useParams } from "react-router-dom";import { useQuery } from "@tanstack/react-query";import { toast } from "sonner";import { coursesApi,type CourseScheduleSlot } from "@/api/coursesApi";import { courseError } from "@/lib/courseUi";import { usePortalAuth } from "@/portal/PortalAuthContext";import DashboardLayout from "@/layouts/DashboardLayout";import { Button } from "@/components/ui/button";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";import { Trash2,Plus } from "lucide-react";
const days:CourseScheduleSlot["day"][]=["saturday","sunday","monday","tuesday","wednesday","thursday","friday"];const labels:Record<string,string>={saturday:"السبت",sunday:"الأحد",monday:"الاثنين",tuesday:"الثلاثاء",wednesday:"الأربعاء",thursday:"الخميس",friday:"الجمعة"};
export default function CourseClassroomSchedule(){const {classroomId=""}=useParams();const {user}=usePortalAuth();const editable=!!user&&["admin","teacher","supervisor"].includes(user.role);const q=useQuery({queryKey:["course-schedule",classroomId],queryFn:()=>coursesApi.getSchedule(classroomId),retry:false});const [timezone,setTimezone]=useState("Africa/Cairo");const [slots,setSlots]=useState<CourseScheduleSlot[]>([]);const [busy,setBusy]=useState(false);useEffect(()=>{if(q.data){setTimezone(q.data.timezone||"Africa/Cairo");setSlots(q.data.slots||[])}},[q.data]);const save=async()=>{setBusy(true);try{await coursesApi.updateSchedule(classroomId,{timezone,slots});toast.success("تم حفظ جدول الفصل");q.refetch();}catch(e){toast.error(courseError(e));}finally{setBusy(false)}};return <DashboardLayout><Card className="mx-auto max-w-4xl"><CardHeader><CardTitle>جدول فصل الدورة</CardTitle></CardHeader><CardContent className="space-y-5">{q.isLoading?<p>جاري التحميل...</p>:q.error?<p className="text-destructive">{courseError(q.error)}</p>:<><label className="block space-y-2"><span>المنطقة الزمنية</span><Input value={timezone} onChange={e=>setTimezone(e.target.value)} disabled={!editable}/></label><div className="space-y-3">{slots.map((s,i)=><div key={i} className="grid gap-2 rounded-xl border p-3 md:grid-cols-[1fr_1fr_1fr_auto]"><Select value={s.day} disabled={!editable} onValueChange={x=>setSlots(v=>v.map((y,n)=>n===i?{...y,day:x as CourseScheduleSlot["day"]}:y))}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{days.map(d=><SelectItem key={d} value={d}>{labels[d]}</SelectItem>)}</SelectContent></Select><Input type="time" value={s.startTime} disabled={!editable} onChange={e=>setSlots(v=>v.map((y,n)=>n===i?{...y,startTime:e.target.value}:y))}/><Input type="time" value={s.endTime||""} disabled={!editable} onChange={e=>setSlots(v=>v.map((y,n)=>n===i?{...y,endTime:e.target.value||undefined}:y))}/>{editable&&<Button size="icon" variant="ghost" onClick={()=>setSlots(v=>v.filter((_,n)=>n!==i))}><Trash2 className="h-4 w-4"/></Button>}</div>)}{!slots.length&&<p className="text-sm text-muted-foreground">لا توجد مواعيد لهذا الفصل.</p>}</div>{editable&&<div className="flex gap-2"><Button variant="outline" onClick={()=>setSlots(v=>[...v,{day:"sunday",startTime:"18:00",endTime:"19:00"}])}><Plus className="me-2 h-4 w-4"/>موعد</Button><Button onClick={save} disabled={busy}>{busy?"جاري الحفظ...":"حفظ الجدول"}</Button></div>}</>}</CardContent></Card></DashboardLayout>}
